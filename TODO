Increments:

- Instead of using glDrawPixels, set up a quad in the viewport and
assign a texture to it; then have OpenCL render to that texture directly

Other:
- Command-line argument processing to set
  * log level and destination
  * device preference (e.g. cpu vs gpu)
